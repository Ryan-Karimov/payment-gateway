apiVersion: v1
kind: Service
metadata:
  name: payment-gateway
  namespace: payment-gateway
  labels:
    app: payment-gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: payment-gateway
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: payment-gateway
  namespace: payment-gateway
